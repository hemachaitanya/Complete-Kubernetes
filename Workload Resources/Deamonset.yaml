---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: jenkins
  labels:
    env: uat
spec:
  minReadySeconds: 2
  selector:
    matchLabels:
      env: uat
  template:
    metadata:
      namespace: dev
      name: pod-spec
      labels:
        env: uat
    spec:
      containers:
        - name: jenkins
          image: jenkins/jenkins
          ports:
            - containerPort: 8080
         